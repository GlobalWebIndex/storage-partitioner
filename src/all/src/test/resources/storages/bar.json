{
  "id": "bar",
  "source": {
    "dataSource": "bar",
    "coordinator": "${USER}",
    "overlord": "${LANGUAGE}",
    "broker": "${SCALA_HOME}",
    "access": "rw",
    "properties": {

    }
  },
  "partitioner": {
    "granularity": "HOUR",
    "pathFormat": "yyyy/MM/dd/HH/mm/ss",
    "pathPattern": "^.*(\\d{4})/(?:(\\d{2})/(?:(\\d{2})/(?:(\\d{2})/(?:(\\d{2})/(?:(\\d{2})/)?)?)?)?)?.*$"
  }
}