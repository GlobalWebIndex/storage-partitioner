{
  "id": "bar",
  "source": {
    "dataSource": "bar",
    "coordinator": "${USER}",
    "overlord": "${USER}",
    "broker": "${USER}",
    "access": "rw",
    "meta": ["version-foo"],
    "properties": {

    }
  },
  "partitioner": {
    "granularity": "HOUR",
    "pathFormat": "yyyy/MM/dd/HH/mm/ss",
    "pathPattern": "^.*(\\d{4})/(?:(\\d{2})/(?:(\\d{2})/(?:(\\d{2})/(?:(\\d{2})/(?:(\\d{2})/)?)?)?)?)?.*$"
  }
}